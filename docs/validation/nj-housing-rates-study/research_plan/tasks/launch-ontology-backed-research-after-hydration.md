---
title: Launch ontology-backed research after hydration
status: cancelled
assigned_role: planner
runner: default
dependencies:
  - verify-hydrated-ontology-health-before-research
acceptance_criteria:
  - planner creates or promotes downstream research tasks that explicitly depend on hydrated ontology artifacts
  - no final analytical claim or dashboard is treated as complete unless ontology-backed queries are available
  - project direction is updated from pre-hydration planning to ontology-backed research execution
related_files:
  - research_plan
  - topics
  - artifacts
latest_run_summary: "Superseded: the corresponding auditor is now `ready` so this repair task is no longer needed. Cancelled by archetype-closeout driver."
---

## Description

Launch ontology-backed research after hydration. This ontology-first project cannot be treated as complete until hydration succeeds, ontology health is verified, and downstream research is explicitly reopened from the hydrated ontology.
