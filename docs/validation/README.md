# Validation Workspaces

This directory is reserved for local validation projects and benchmark runs. Its contents are ignored by default because validation sessions often contain generated ontology databases, local runner traces, large artifacts, and provider-specific state.

Commit only small, curated summaries to regular docs. Keep full validation workspaces local or publish them in a separate fixture repository when they are intentionally part of a release.
