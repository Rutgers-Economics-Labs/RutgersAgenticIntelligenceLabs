# Work Orders Moved

The active blank-slate implementation queue now lives in `state/work-orders/`.

This directory is intentionally empty except for this pointer so older goal-era work orders do not compete with the planner-first architecture.

